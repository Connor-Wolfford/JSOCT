//Literals

//Javascript defines two types of values, fixed values, and variable values

//Fixed values are called literals, and variable values are called variables

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days.toString());
console.log(days.length)

let tired = true;

let soup = {
    a: 'Chicken Noodle',
    b: 'Tomato',
    c: 'Beef and Barley'
}

console.log(soup.a, ' ', soup.c, soup.c.length)

let car = 'Honda';

let dec = 10.2;